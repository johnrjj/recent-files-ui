import styled from 'styled-components';
import { BarChart } from 'react-feather';

const Logo = styled(BarChart)`
  margin-bottom: 2rem;
  margin-left: -0.5rem;
  height: 3rem;
  width: 3rem;
`;

export { Logo };
